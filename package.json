{
  "name": "mold",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:native": "cd native && zig build",
    "start": "bun run build:native && cd examples/hero && bun run dev"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ],
    "catalog": {
      "typescript": "^5.8.3"
    }
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "winston": "^3.17.0"
  }
}
